defaultNamespace: default
namespace: default

namespaceLabels:
  app: metallb
namespaceAnnotations:
  app: metallb

helm:
  releaseName: metallb
  chart: metallb
  repo: https://metallb.github.io/metallb
  version: 0.14.5

  valuesFiles:
    - "metallb-values.yaml"

  